{
  "scripts": {
    "dev": "vitepress dev docs --host=127.0.0.1 --port=5173",
    "build": "vitepress build docs",
    "serve": "vitepress serve docs",
    "push": "git push -u origin && git push -u overtrue"
  },
  "devDependencies": {
    "vitepress": "^1.0.0-alpha.13",
    "vue": "^3.2.38"
  }
}
